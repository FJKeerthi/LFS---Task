[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
dependsOn = ["setup"]
cmds = [
    "npm run install-server",
    "npm run install-client"
]

[phases.build]
dependsOn = ["install"]
cmds = [
    "npm run build"
]

[start]
cmd = "npm start"
