{
  "name": "weather-fullstack-app",
  "version": "1.0.0",
  "description": "Weather application with React frontend and Express backend",  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd client && npm start",
    "dev": "cd client && npm start"
  },
  "keywords": ["react", "express", "weather", "fullstack"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "mongoose": "^7.0.0"
  },  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
